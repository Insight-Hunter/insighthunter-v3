
{
  "name": "insighthunter-v3",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js --platform=neutral --target=es2020",
    "deploy": "npm run build && npx wrangler deploy --config insight-hunter-backend/wrangler.toml"
  },
  "dependencies": {
    "@tsndr/cloudflare-worker-jwt": "^3.2.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "esbuild": "^0.25.10",
    "wrangler": "^4.39.0"
  }
}
