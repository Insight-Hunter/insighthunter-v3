{
  "name": "insight-hunter-frontend",
  "private": true,
  "scripts": {
    "build": "tsc -b && vite build",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name insight-hunter-frontend"
  },
  "devDependencies": {
    "vite": "^6.3.6",
    "typescript": "^5.2.0",
    "wrangler": "^4.39.0"
  }
}
